# /etc/nginx/sites-available/aut  -> sites-enabled 에 링크
server {
    listen 80 default_server;
    server_name _;
    root /home/ubuntu/aut.stock/dashboard;
    index index.html;
    location / { try_files $uri $uri/ =404; }
    add_header Cache-Control "no-store";
}
